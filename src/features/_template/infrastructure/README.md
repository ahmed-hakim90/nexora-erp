# Infrastructure Layer

Owns repository implementations, Supabase access, mappers, storage adapters, and external integration adapters.

Infrastructure is server-only and must never be imported by React components.
