# Presentation Layer

Owns React components, forms, hooks, tables, and view models for this feature.

Presentation code renders authorized data and captures intent; it must not contain business rules or Supabase queries.
